[package]
name = "argus-sandbox"
version = "0.1.0"
edition = "2021"
authors = ["R Jan Steve Daniel"]
description = "ARGUS 2.0 Linux Namespace, Seccomp, and POSIX Process Sandbox Supervisor"

[dependencies]
argus-core = { path = "../argus-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
